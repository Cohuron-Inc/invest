{
  "name": "invest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Daily X-feed research corpus for a curated market-commentary allowlist.",
  "packageManager": "pnpm@8.15.1",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src --max-warnings=0",
    "clean": "rm -rf dist node_modules",
    "task:resolve-accounts": "tsx --env-file-if-exists=.env src/x/resolve-accounts.ts",
    "task:sync-universe": "tsx --env-file-if-exists=.env src/lib/universe.ts",
    "task:capture": "tsx --env-file-if-exists=.env src/capture/index.ts",
    "task:backfill": "tsx --env-file-if-exists=.env src/capture/backfill.ts",
    "task:normalize": "tsx --env-file-if-exists=.env src/normalize/index.ts",
    "task:extract": "tsx --env-file-if-exists=.env src/extract/index.ts",
    "task:session-dump": "tsx --env-file-if-exists=.env src/extract/session/dump.ts",
    "task:session-ingest": "tsx --env-file-if-exists=.env src/extract/session/ingest.ts",
    "task:enrich": "tsx --env-file-if-exists=.env src/prices/index.ts",
    "task:render": "tsx --env-file-if-exists=.env src/render/index.ts",
    "rebuild": "tsx --env-file-if-exists=.env src/rebuild.ts",
    "repl": "tsx --env-file-if-exists=.env src/duck/repl.ts",
    "q": "tsx --env-file-if-exists=.env src/duck/q.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.124.0",
    "@duckdb/node-api": "^1.5.5-r.4",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.4.1",
    "eslint": "^10.10.0",
    "globals": "^17.12.0",
    "tsx": "^4.23.13",
    "typescript": "~5.9.3",
    "typescript-eslint": "^8.69.0",
    "vitest": "~4.1.11"
  }
}
